#!/bin/sh
# NumbahWan TCG - Pre-commit hook
# Auto-updates RAG cache before each commit

echo "🔄 Updating RAG cache..."
node scripts/rag-optimizer.cjs --quick 2>/dev/null

# Stage the updated context file if it changed
git add .ai-context.md 2>/dev/null || true

echo "✅ RAG cache updated"
