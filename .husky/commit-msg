#!/bin/sh
# Mycelium: commit-msg — failure-intake reminder (advisory, never blocks)
# A fix commit that doesn't add a failure task lets reality's lesson evaporate.
if [ -f "tools/failure-intake-lint.cjs" ]; then
  node "tools/failure-intake-lint.cjs" "$1" 2>&1 >&2 || true
fi
exit 0
