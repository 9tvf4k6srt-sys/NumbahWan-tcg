# NumbahWan TCG - AI Context (Compressed)

## Quick Reference
```
Sandbox: https://3000-i8pqrv0s1id0a05f7rae2-583b4d74.sandbox.novita.ai
GitHub: https://github.com/9tvf4k6srt-sys/NumbahWan-tcg
Stack: Hono + Vite + Cloudflare Pages | Vanilla JS + Tailwind CDN
```

## Commands
```bash
npm run build && pm2 restart numbahwan-guild  # Build+restart
curl http://localhost:3000/api/health          # Test
git add . && git commit -m "msg" && git push   # Deploy
```

## File Locations
| Type | Path |
|------|------|
| Pages | `public/*.html` |
| JS | `public/static/*.js` |
| Data | `public/static/data/*.json`, `src/data/*.json` |
| API | `src/index.tsx` |

## Key Systems
| System | Global | File |
|--------|--------|------|
| Wallet | `NW_WALLET` | `nw-wallet.js` |
| Cards | `NW_CARDS` | `nw-cards.js` |
| Nav | `NW_NAV` | `nw-nav.js` |
| Sounds | `NW_SOUNDS` | `nw-sounds.js` |

## Currencies
diamond(premium), gold, iron, stone, wood(sacred logs)

## DB Tables (D1)
citizens(device_uuid) -> wallets(citizen_id) -> businesses(owner_device_uuid)

## Rules
- NO emojis in UI - use `/static/icons/nw-icons.svg`
- Z-index scale: base:1, dropdown:100, sticky:200, fixed:300, nav:500, drawer:600, modal:700, overlay:800, toast:900
- Top 60px reserved for header, bottom 80px for FABs

## Current Task
Create unified `NW_USER` system wrapping citizen+wallet+business into one interface.
